/// Mock implementation of the web URL strategy for testing purposes.
///
/// This mock implementation allows testing code that depends on the URL
/// strategy without requiring the actual web implementation.
library;

/// Mock implementation of the usePathUrlStrategy function.
///
/// This function mimics the behavior of the web implementation but doesn't
/// actually depend on web-specific code.
void usePathUrlStrategy() {
  // Mock implementation that doesn't do anything but can be called
  // in tests without throwing exceptions
}
